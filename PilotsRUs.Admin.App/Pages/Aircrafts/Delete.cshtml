@page "{id:guid}"
@model PilotsRUs.Admin.App.Pages.Aircrafts.DeleteModel
@{
    ViewData["Title"] = "Delete aircraft";
}

<div class="card">
    <div class="card-body">
        @if (Model.ErrorMessage is not null)
        {
            <div class="alert alert-danger" role="alert">@Model.ErrorMessage</div>
        }

        @if (Model.TargetAircraft is not null)
        {
            <p>Delete <strong>@Model.TargetAircraft.RegistrationNumber</strong>? This cannot be undone.</p>

            <form method="post">
                <button type="submit" class="btn btn-danger">Delete</button>
                <a asp-page="/Aircrafts/Index" class="btn btn-secondary">Cancel</a>
            </form>
        }
    </div>
</div>
