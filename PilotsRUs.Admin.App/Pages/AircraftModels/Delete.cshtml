@page "{id:guid}"
@model PilotsRUs.Admin.App.Pages.AircraftModels.DeleteModel
@{
    ViewData["Title"] = "Delete aircraft model";
}

<div class="card">
    <div class="card-body">
        @if (Model.ErrorMessage is not null)
        {
            <div class="alert alert-danger" role="alert">@Model.ErrorMessage</div>
        }

        @if (Model.TargetModel is not null)
        {
            <p>Delete <strong>@Model.TargetModel.ManufacturerName @Model.TargetModel.Name</strong>? This cannot be undone.</p>

            <form method="post">
                <button type="submit" class="btn btn-danger">Delete</button>
                <a asp-page="/AircraftModels/Index" class="btn btn-secondary">Cancel</a>
            </form>
        }
    </div>
</div>
